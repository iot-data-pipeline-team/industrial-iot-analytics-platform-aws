# Git Workflow

# Check the current status
git status

# See the current branch
git branch

# Stage all changes
git add .

# Commit your changes
git commit -m "Describe your changes"

# Push to your branch
git push origin aws-cloud-migration-malek